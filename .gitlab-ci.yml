image: maven:3-jdk-8-alpine

stages:
  - compile
  - test
  - checkstyle

build:
  stage: compile
  script:
    - mvn compile

test:
  stage: test
  script:
    - mvn test

checkstyle:
  stage: checkstyle
  script:
    - mvn validate
