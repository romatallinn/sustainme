image: maven:3.6.0-jdk-11

stages:
  - compile
  - test
  - checkstyle

build:
  stage: compile
  script:
    - mvn compiler:compile

test:
  stage: test
  script:
    - mvn surefire:test

checkstyle:
  stage: checkstyle
  script:
    - mvn checkstyle:check
